/**
 * @desc Routes for the whole app.
 * @const
 * @type {object}
 * @default
 */

export const SERVER_URL = "";

export const ROUTES = {
  root: "/",
  cart: "/cart",
  contacts: "/contacts",
  guarantees: "/guarantee",
  goods: "/goods",
};
